# Gmail Support Inbox Sync

IMAP-based email fetch client:

* **Inbox Location**: Connects to support address `vamshikrishna5108@gmail.com`.
* **IMAP Parsing**: Loops unread items, isolates body text, classifies sentiment via keyword heuristics, and logs records to SQL tables.